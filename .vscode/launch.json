{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Current File (from root)",
			"type": "debugpy",
			"request": "launch",
			"program": "${file}",
			"cwd": "${workspaceFolder}",
			"env": {
				"PYTHONPATH": "${workspaceFolder}"
			},
			"console": "integratedTerminal"
		}
	]
}
